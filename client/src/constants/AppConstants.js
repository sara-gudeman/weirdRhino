var keyMirror = require('keymirror');

var AppConstants = {
  ActionTypes: keyMirror({
    SUBMIT_SEARCH: null,
    // PRODUCT_QUERY: null,
    USER_LOGIN: null
  })
};

module.exports = AppConstants;
